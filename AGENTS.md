# 项目协作规则

## 通用要求

- 对话时使用中文。
- 不要主动生成代码改动说明文档。
- 除非用户明确要求，否则永远不要对生产环境进行任何操作。
- 保留用户已有代码和未提交修改，不得擅自执行破坏性 Git 操作。

## 新游戏开发强制流程

当任务涉及“新建游戏、添加游戏、制作游戏、重做游戏、增加新玩法”时，必须执行以下流程。

### 1. 开始前加载规范

在修改任何游戏代码前，必须完整阅读：

```text
docs/GAME_DEVELOPMENT_STANDARD.md
```

不得只根据规范标题、摘要或以往记忆开始开发。

### 2. 先完成游戏简报

开始实现前，必须根据规范第 3 章明确以下内容：

- 游戏名称与类型。
- 一句话核心循环。
- 主要输入方式。
- 成功与失败条件。
- 目标单局时长。
- 前三分钟体验。
- 关卡或成长结构。
- 美术与声音方向。
- 最高负载场景及性能风险。

用户已经明确的信息直接采用；缺失但不影响方向的信息可以作出合理假设，并在执行计划中说明。

### 3. 先规划后执行

开发计划必须覆盖以下阶段：

1. 规则原型。
2. 垂直切片。
3. 内容与数值完善。
4. 性能优化与最终验收。

不得先批量制作素材、剧情或关卡，再补核心规则。

### 4. 强制分层结构

每款游戏使用独立目录：

```text
features/games/<game-name>/
```

根据游戏复杂度拆分以下职责：

```text
Game.tsx             React 界面、菜单和 HUD
Game.module.css      独立样式
Engine.ts            实时主循环和高频状态
rules.ts             可独立测试的规则
types.ts             类型定义
data.ts              数值与关卡配置
assets.ts            素材注册
audio.ts             专属音频
```

简单游戏可以合并少量文件，但禁止把规则、引擎、UI、配置、音频和大量样式全部放进一个文件。

### 5. 开发过程必须应用的规范

必须遵守制作规范中的：

- 核心循环和前三分钟体验要求。
- 操作响应、坐标换算与触屏适配要求。
- 显式状态机与暂停规则。
- 数值、任务、成长和难度预算。
- 关卡、Boss、AI 和剧情要求。
- UI 信息层级与弹窗规则。
- 美术统一、素材授权与 AI 素材处理流程。
- 特效、声音和同屏预算。
- React 与实时游戏引擎的职责边界。
- RAF、定时器、事件监听与资源清理要求。
- 60 FPS 性能预算和高负载压测要求。
- 对应游戏类型的专项规范。

### 6. 平台接入要求

新游戏必须接入：

- 大厅游戏注册。
- 游戏分类与搜索。
- 游戏卡片美术。
- 最近游玩。
- 最高分与玩家排名。
- 全屏与全局静音。
- 准确的操作说明。

游戏成绩通过公共 SDK 提交，不得直接修改其他游戏的存档。

### 7. 完成前强制验收

完成后必须按照制作规范第 18 章 `Definition of Done` 逐项检查，并至少完成：

```text
npm run lint
npm run build
```

还必须在本地 `http://localhost:4399/` 实际试玩，验证：

- 可以从大厅打开。
- 可以开始、暂停、胜利或失败并重新开始。
- 核心操作真实生效。
- 全屏后输入坐标正确。
- 最高分正常记录。
- 高负载场景仍可玩。
- 浏览器控制台没有错误。

涉及纯规则的游戏，还必须对正常操作、非法操作、边界条件、连锁情况和胜负结算进行规则级测试。

### 8. 冲突处理

- 用户的明确要求高于通用制作规范。
- 当用户要求与规范冲突时，应指出会产生的影响，并按用户最终决定执行。
- 不得以规范为理由擅自删减用户明确要求的核心玩法。
- 不得为了形式上完成检查而伪造测试、性能数据或试玩结果。
